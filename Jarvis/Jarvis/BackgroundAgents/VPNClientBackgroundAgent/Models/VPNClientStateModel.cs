namespace Jarvis;

public class VPNClientStateModel : ClientStateModelBase
{
    public VPNClientStateModel(
        string title,
        string subtitle,
        bool isActive)
        : base(title, subtitle, isActive)
    {
    }
}
